# --- API Server Configuration ---
PORT=3000
NODE_ENV=development

# --- Postgres Database (PostgreSQL) ---
# Format: DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dexorderexecutionengine

# --- Redis Configuration ---
# Used for BullMQ, Caching, and Pub/Sub
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# --- WebSocket Configuration ---
# The separate broadcast server port
BROADCAST_PORT=8080